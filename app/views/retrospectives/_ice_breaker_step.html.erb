<div class="bg-gradient-to-br from-blue-50 to-purple-50 rounded-2xl shadow-lg border border-blue-100 p-12 mb-8">
  <div class="text-center max-w-3xl mx-auto">
    <!-- Ice Breaker Icon -->
    <div class="w-20 h-20 mx-auto mb-6 bg-blue-500 rounded-full flex items-center justify-center">
      <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
    </div>

    <h2 class="text-2xl font-bold text-gray-900 mb-4">Ice Breaker</h2>
    <p class="text-gray-600 mb-8">Let's warm up with a fun question to get everyone talking!</p>

    <!-- Question Display -->
    <%= render partial: 'retrospectives/ice_breaker_question', locals: { question: retrospective.current_ice_breaker_question } %>

    <!-- Leader Controls -->
    <% if is_leader %>
      <div class="flex justify-center gap-4">
        <%= button_to change_ice_breaker_question_retrospective_path(retrospective),
                      method: :post,
                      class: "px-6 py-3 border border-transparent text-sm font-medium rounded-xl text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-all duration-200 shadow-lg" do %>
          <svg class="w-5 h-5 inline-block mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Change Question
        <% end %>

        <%= button_to advance_step_retrospective_path(retrospective),
                      method: :post,
                      class: "px-6 py-3 border border-transparent text-sm font-medium rounded-xl text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition-all duration-200 shadow-lg" do %>
          Continue to Next Step
          <svg class="w-5 h-5 inline-block ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        <% end %>
      </div>
    <% else %>
      <p class="text-gray-500 italic">
        Waiting for <%= retrospective.creator.username %> to start the next step...
      </p>
    <% end %>
  </div>
</div>
